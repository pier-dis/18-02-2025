
fun main() {
/*
   var cc= Contocorrente(300.0)
   cc.deposita(1000.0)
   cc.preleva(400.0)
 */

   /*
   var a=ComplexNumber(1.5,2.5)
   var b=ComplexNumber(3.5,-4.5)
   var somma=a+b
   println("parte reale: ${somma.real},parte immaginaria: ${somma.imaginary}")
    */
/*
   var p = Point(2.8, 1.5)
   var x=readln().toDouble()
   var y=readln().toDouble()
   var p2= Point(x, y)
   var volume=Cilinder(3.0,5.0)
   var area=Cilinder(3.0,5.0)
   println("x: ${p.x}, y: ${p.y}")
   println("La distanza tra il punto e l'origine e' ${p.distanceToOrigin()}")
   println("La distanza tra il punto e l'origine e' ${p.distance(p2)}")
   println("volume del cilindro: ${volume.calcolaVolume()}")
   println("l'area del cilindro: ${area.calcolaAreaSuperficiale()}")
*/

    var a:Polygon=triangolo (4.3)
    var lista = mutableListOf()
    lista.add(Quadrato(5.0))
    lista.add(triangolo(5.0))


}